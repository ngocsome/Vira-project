spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/vira?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:vira_app}
    password: ${DB_PASSWORD:vira_app_dev}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false

logging:
  level:
    org.springframework.security: INFO
    vn.vira: DEBUG
